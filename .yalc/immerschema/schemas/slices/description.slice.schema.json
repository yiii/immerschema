{
  "$id": "immerschema/schemas/slices/description.slice.schema.json",
  "title": "Narrative description slice",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "One-paragraph narrative of what the viewer sees/hears. Markdown allowed."
    },
    "descriptionLang": {
      "type": "string",
      "pattern": "^[a-z]{2}$",
      "default": "en",
      "description": "ISO-639-1 language code of the description text."
    }
  },
  "required": ["description"],
  "additionalProperties": true
} 